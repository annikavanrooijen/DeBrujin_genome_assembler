# DeBrujin_genome_assembler
The project implements a de Bruijn graph–based genome assembler that reconstructs genomic sequences from short reads by modeling k-mer overlaps and extracting contigs from the resulting graph.
